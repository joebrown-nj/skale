<button 
    class="{if $class}{$class}{else}btn btn-primary w-100{/if}" 
    data-bs-toggle="modal" 
    data-bs-target="#staticBackdrop" 
    aria-describedby="{$describedBy} website development lead gen page" 
    data-meta-custom-event="{$metaEvent} LandingLeadCtaClick" 
    data-meta-label="{$metaLabel}"
>
    {$text}
</button>